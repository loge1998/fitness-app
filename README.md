# fitness-app

This repository contains code for backend. It exposes apis of users, goals and tasks needed for the frontend.

To run this app, Do `uvicorn app.main:app --port 5000`

Also we have deployed it to heroku. Link to the deployed application : https://fit-ness-app.herokuapp.com/docs